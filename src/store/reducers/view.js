
let defaultState = {
    width: "40px",
    height: "40px",
    marginLeft: "40px"
}

export const view = (state = defaultState, action) => {

    switch (action.type) {
        default:
            return state
    }
}